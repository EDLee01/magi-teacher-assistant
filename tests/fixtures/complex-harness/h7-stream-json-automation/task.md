Create a file and report the path.

Requirements:
- Create `output/automation-result.txt` with the exact text `stream-json automation ok`.
- Do not change package.json, generated files, or files outside this repo.
- Keep the final answer concise and include the created path.
