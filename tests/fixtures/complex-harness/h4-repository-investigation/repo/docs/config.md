# Config Loader

`server.port` accepts any integer from 0 to 65535. Port `0` asks the operating
system to choose an available port.

`client.retryLimit` accepts any integer from 0 to 10. A retry limit of `0`
disables retries.
